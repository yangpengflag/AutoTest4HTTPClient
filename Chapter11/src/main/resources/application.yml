server:
    port: 8081

logging:
  path: logs
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.10.1.80:3306/course
    username: root
    password:

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/*
